export const DEFAULT_MODAL_TIMESTAMPTS = {
  timestamps: { createdAt: "created_at", updatedAt: "updated_at" },
};

export const FIELD_REQUIRED_MSG = "{VALUE} is required.";

export const REQUIRED_FIELD_MSG = "This field is required";
